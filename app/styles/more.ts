import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	versionApp: {
		textAlign: 'center',
		color: 'rgb(107 114 128)',
		opacity: 0.5,
		marginTop: 16,
		marginBottom: 16,
	},
})
